- form_for @category do |f|
  = f.error_messages

  = f.label :name, "Name:"
  = f.text_field :name

  = f.label :display_order, "Reihenfolge"
  = f.select :display_order, array_for_order_select_field
  
  = f.submit "speichern"